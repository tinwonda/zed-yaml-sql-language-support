title: demo

#language=sql
query: SELECT id, email FROM users WHERE active = true

sql_query: |
  SELECT id, created_at
  FROM orders
  WHERE status = 'paid'
  ORDER BY created_at DESC

metadata:
  query_sql: |
    SELECT count(*)
    FROM sessions
    WHERE expired_at IS NULL

  not_sql: this should stay plain yaml
